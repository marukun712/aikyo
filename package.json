{
  "name": "aikyo",
  "type": "module",
  "private": true,
  "scripts": {
    "companion": "npx tsx ./configs/$npm_config_config/companion.ts",
    "router": "npx tsx ./routing/index.ts",
    "firehose": "npx tsx ./firehose/index.ts",
    "format": "prettier --write .",
    "lint": "oxlint ."
  },
  "workspaces": [
    "server",
    "firehose",
    "configs",
    "apm_tools",
    "routing",
    "packages/utils"
  ],
  "devDependencies": {
    "oxlint": "^1.14.0",
    "prettier": "^3.6.2"
  }
}
